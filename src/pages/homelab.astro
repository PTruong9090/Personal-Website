---
import { getEntry, render } from 'astro:content';
import CaseStudy from '../layouts/CaseStudy.astro';

const entry = await getEntry('projects', 'homelab');
const { Content } = await render(entry!);
---

<CaseStudy entry={entry!}>
  <Content />
</CaseStudy>
